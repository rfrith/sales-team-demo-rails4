<%= form_for(@sales_rep) do |f| %>
  <% if @sales_rep.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sales_rep.errors.count, "error") %> prohibited this sales_rep from being saved:</h2>

      <ul>
      <% @sales_rep.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Unique Identifier" %><br>
    <%= f.text_field :unique_id %>
  </div>
  <div class="field">
    <%= f.label :first_name %><br>
    <%= f.text_field :first_name %>
  </div>
  <div class="field">
    <%= f.label :last_name %><br>
    <%= f.text_field :last_name %>
  </div>
  <div class="field">
    <%= f.label :sales_group_id %><br>
    <% f.text_field :sales_group_id %>
	
  	<%= f.select(:sales_group_id, SalesGroup.order(:unique_id).collect {|sg| [ sg.unique_id, sg.id ] } , {include_blank: 'None'})%>
  </div>
  <div class="actions">
    <%= f.submit "Create Sales Rep" %>
  </div>
<% end %>